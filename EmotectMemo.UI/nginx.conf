
events {
}

http {
	include /etc/nginx/mime.types;

	server {
		listen 80;

        location / {
            root /usr/share/nginx/code/src;
            try_files $uri $uri/ /index.html;
        }
		location /health {
			return 200 'alive';
			add_header Content-Type text/plain;
		}
	}
}